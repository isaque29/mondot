unit Program
{
    on MdReload -> ()
        local desc = 'A while loop';
        local n = sub(0, 100000);

        while (1)
            n = add(n, 1);
            Print(n);

            if (n)
            else
                return 0;
            end
        end
    end
}
