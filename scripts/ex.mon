unit Ex
{
    on MdInit -> ()
        msg = 'unit loaded. executing...';
        Print(msg);
    end

    on MdReload -> ()
        Print('here i\'m about run the program');
        Print('here i\'m about run the program');
        
        y = Spawn('y');
        n = Spawn('n');
        Print(x);
        
    end
}
