unit Ex
{
    on MdReload -> ()
        local desc = 'A if statement';
        
        local showld = true;
        if (showld)
            io.print('it\'s true!');
            return 0;
        end

        io.print('false..');
    end
}
